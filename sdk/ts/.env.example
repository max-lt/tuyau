# Copy to .env and fill in. The tuyau-server prints SERVER + FINGERPRINT on startup.
TUYAU_SERVER=host:4433
TUYAU_FINGERPRINT=<64 hex chars: server tunnel cert SHA-256>
TUYAU_TOKEN=<64 hex chars: pre-shared token>
TUYAU_HOSTNAME=your.example.com
